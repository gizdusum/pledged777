import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Pledged 777 — Ritual Testnet Genesis Registry",
  description:
    "777 permanent on-chain pledges on Ritual Testnet. Zero gas required — the relayer covers all fees. Connect your wallet, upload an image, leave your mark forever.",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
